# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                               microservice.ServiceController.index
GET     /wordCount/:word                microservice.ServiceController.getWordCount(word: String)
POST    /wordCount                      microservice.ServiceController.postWords
